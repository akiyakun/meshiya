<li>
  <%= gravatar_for user, size: 52 %>
  <%= link_to user.name, user %>
  <%= if user.admin then t('w.admin') end %>

  <% if current_user.admin? && !current_user?( user ) %>
    | <%= link_to t('.delete'), user, method: :delete, data: { confirm: "You sure?" } %>
  <% end %>
</li>
