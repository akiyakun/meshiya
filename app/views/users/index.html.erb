<% provide( :title, t('.title') ) %>
<h1><%= t('.title') %></h1>

<%= will_paginate previous_label: t('w.previous'), next_label: t('w.next') %>

<ul class="users">
  <% @users.each do |user| %>
    <%= render user %>
  <% end %>
</ul>

<%= will_paginate previous_label: t('w.previous'), next_label: t('w.next') %>
